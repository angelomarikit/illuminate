# Illuminate Client (Expo)

See **[docs/MOBILE_EXPO.md](../docs/MOBILE_EXPO.md)** for full local + EAS / App Store / Play Store instructions.

Quick start:

```bash
cp .env.example .env   # same Supabase URL + anon key as web
npm install
npx expo start
```
